# Test environment — committed to repo, no secrets
# Note: vitest.config.ts dynamically sets DATABASE_URL using the system username.
# This file is for reference only. The actual URL is set in vitest.config.ts.
DEV_MODE="true"
