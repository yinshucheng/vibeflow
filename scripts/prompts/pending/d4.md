切到 feature/f2-llm-engine 分支。

任务: D4 — S11.6 国产 LLM 验证

目标: 端到端验证 Qwen / Kimi 的 Tool Use + 流式输出

必读文件:
- src/config/llm.config.ts — MODEL_REGISTRY（Qwen/Kimi/SiliconFlow 已注册）
- src/services/llm-adapter.service.ts — callLLM 封装
- tests/services/llm-adapter.service.test.ts — 现有 mock 测试

实现:
1. 创建集成测试文件 tests/integration/llm-providers.test.ts:
   - 对每个国产模型 (Qwen, Kimi, SiliconFlow):
     a. 基本对话: 发送简单中文消息，验证收到非空回复
     b. 流式输出: 验证 onDelta 回调被多次调用
     c. Tool Use: 定义一个测试 tool，验证模型能正确调用
     d. 中文处理: 验证中文输入/输出不乱码
   - 如果 API key 不存在，test.skip() (不阻塞 CI)
   - 超时设置 30s per test
2. 创建兼容性矩阵文档 docs/ai-chat-design/llm-compatibility.md:
   - 表格: 模型 × 能力 (基本对话/流式/Tool Use/中文)
   - 已知限制和注意事项
3. 在 docs/ai-chat-design/pending-tasks.md 中 D4 的 `- [ ]` 改为 `- [x]` 并 commit

验证命令:
  npx vitest run tests/integration/llm-providers.test.ts

注意:
- 集成测试需要真实 API key，没有 key 时 graceful skip
- 不要在测试中硬编码 API key，从环境变量读取
- 确保 npx tsc --noEmit 通过后 commit
