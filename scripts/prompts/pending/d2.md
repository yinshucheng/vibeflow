切到 feature/f2-llm-engine 分支。

任务: D2 — S11.2 对话搜索

目标: 全文搜索归档对话内容

必读文件:
- prisma/schema.prisma — ChatMessage model
- src/services/chat.service.ts — 现有查询方法
- src/server/routers/ — 现有 router 模式

实现:
1. 在 chat.service.ts (或新建 chat-search.service.ts) 中新增:
   - `searchMessages(userId, query, options?)`: 全文搜索用户的聊天消息
   - options: { limit?, offset?, conversationId?, dateRange? }
   - 使用 Prisma 的 contains 或 PostgreSQL 全文搜索
   - 验证 userId 所有权
2. 新增 tRPC router 端点:
   - `chat.search` — protectedProcedure
3. 编写测试:
   - tests/services/chat-search.test.ts: 搜索命中、搜索未命中、跨会话搜索、userId 隔离
4. 在 docs/ai-chat-design/pending-tasks.md 中 D2 的 `- [ ]` 改为 `- [x]` 并 commit

注意:
- 遵循 ServiceResult<T> 模式
- 注册到 src/services/index.ts
- Zod schema 定义一次，tRPC router 复用
- 确保 npx tsc --noEmit 通过，npm test 新测试全绿后 commit
