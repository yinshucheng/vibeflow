切到 feature/f2-llm-engine 分支。

任务: D1 — S11.1 Topic Mode 会话

目标: 支持手动创建跨天专题会话（不在 daily archive 时归档）

必读文件:
- prisma/schema.prisma — Conversation model（已有 ConversationType: DEFAULT | DAILY | TOPIC）
- src/services/chat.service.ts — 会话管理逻辑
- src/services/chat-archive.service.ts — archive 逻辑（需排除 TOPIC 类型）
- docs/ai-chat-design/design.md — 设计文档中 Topic Mode 相关设计

实现:
1. 在 chat.service.ts 中新增:
   - `createTopicConversation(userId, title)`: 创建 type=TOPIC 的会话
   - `listTopicConversations(userId)`: 列出用户的 TOPIC 类型会话
   - `switchConversation(userId, conversationId)`: 切换活跃会话
2. 在 chat-archive.service.ts 中修改:
   - archive 逻辑排除 type=TOPIC 的会话
3. 新增 tRPC router (或在现有 chat router 中添加):
   - `chat.createTopic` / `chat.listTopics` / `chat.switchConversation`
4. 编写测试:
   - tests/services/chat-topic.test.ts: CRUD + archive 排除验证
5. 在 docs/ai-chat-design/pending-tasks.md 中 D1 的 `- [ ]` 改为 `- [x]` 并 commit

注意:
- 遵循 ServiceResult<T> 模式
- Service 验证 userId 所有权
- 注册到 src/services/index.ts
- 确保 npx tsc --noEmit 通过，npm test 新测试全绿后 commit
