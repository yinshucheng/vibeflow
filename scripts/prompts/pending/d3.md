切到 feature/f2-llm-engine 分支。

任务: D3 — S11.3 Attachment 引用

目标: 任务长按 → "问 AI" → 携带任务上下文发送到 Chat

必读文件:
- src/types/octopus.ts — ChatAttachmentSchema 已定义
- src/services/chat.service.ts — handleMessage 参数
- vibeflow-ios/src/types/chat.ts — iOS 侧 ChatAttachment 类型
- vibeflow-ios/src/stores/chat-store.ts — iOS chat store

实现:
1. 服务端 (src/services/):
   - 扩展 chatService.handleMessage 支持 attachments 参数
   - 当收到 attachment(type=task) 时，从 DB 查询任务详情并注入到 LLM 上下文
   - 支持的 attachment 类型: task, project, pomodoro
2. Socket 传输:
   - CHAT_MESSAGE event payload 新增 attachments 字段
   - src/server/socket.ts 处理 CHAT_MESSAGE 时传递 attachments
3. iOS 端 (vibeflow-ios/):
   - 在任务详情页添加 "问 AI" 按钮
   - 点击后构造 ChatAttachment 并发送到 chat store
4. 编写测试:
   - tests/services/chat-attachment.test.ts: attachment 注入验证
5. 在 docs/ai-chat-design/pending-tasks.md 中 D3 的 `- [ ]` 改为 `- [x]` 并 commit

注意:
- 遵循 ServiceResult<T> 模式
- attachment 数据不直接发给 LLM，先从 DB 重新查询（安全）
- 确保 npx tsc --noEmit 通过，npm test 新测试全绿后 commit
