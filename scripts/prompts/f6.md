切到 feature/f2-llm-engine 分支。
阅读 docs/ai-chat-design/tasks.md 中的「F6. 上下文管理」部分 (F6.1-F6.3)。
阅读 docs/ai-chat-design/design.md 中与 Context Management / System Prompt / 滑动窗口相关的部分。

实现:
- F6.1 buildSystemPrompt(userId): 静态模板（角色定义 + 行为准则）+ 动态上下文，复用 contextProviderService.getFullContext(userId)
- F6.2 buildLLMMessages(userId, conversationId, newMessage): DB 取最近 N=20 条消息，跳过 role='system'，加入新用户消息。Token 裁剪: 超出预算时从最早的 recent 消息开始移除
- F6.3 所有测试:
  - tests/services/chat-context.test.ts: buildSystemPrompt 返回值包含动态数据; buildLLMMessages 只取最近 20 条, 跳过 system, 新消息追加末尾; Token 裁剪逻辑
  - tests/property/chat-sliding-window.property.ts: 任意消息数 N → 返回 <= min(N, 20) + 1 条; 不包含 role='system'

注意:
- Service 遵循 ServiceResult<T> 模式，参考现有 service 写法
- 使用 tests/helpers/chat-test-setup.ts (F0 已提供)
- 注册到 src/services/index.ts
- 完成后更新 docs/ai-chat-design/tasks.md 中 F6 的 checkbox 为 [x] 并附上 commit hash

确保 npx tsc --noEmit 通过，npm test 中新测试全绿后 commit。
