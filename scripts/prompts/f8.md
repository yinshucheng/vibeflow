切到 feature/f2-llm-engine 分支。
阅读 docs/ai-chat-design/tasks.md 中的「F8. 端侧框架」部分 (F8.1-F8.6)。
阅读 docs/ai-chat-design/design.md 中与 iOS Chat UI / Chat Store / Chat Service (iOS) 相关的部分。
阅读 vibeflow-ios/CLAUDE.md 了解 iOS 项目约定。

实现:
- F8.1 vibeflow-ios/src/types/chat.ts: ChatMessage, PendingToolCall, ChatAttachment, ChatResponsePayload 等类型定义
- F8.2 Chat Store (Zustand) vibeflow-ios/src/store/chat.store.ts: isPanelOpen / panelHeight / messages / isStreaming / streamingContent / pendingToolCalls。Actions: sendMessage, appendStreamDelta, finalizeStreamMessage, confirmToolCall, cancelToolCall, openPanel, closePanel
- F8.3 Chat Service vibeflow-ios/src/services/chat.service.ts: 监听 Socket.io 命令 (CHAT_RESPONSE, CHAT_TOOL_CALL, CHAT_TOOL_RESULT, CHAT_SYNC)，发送事件 (CHAT_MESSAGE, CHAT_ACTION)，initialize() / cleanup() 生命周期
- F8.4 Chat UI 骨架组件: ChatFAB.tsx (浮动按钮), ChatPanel.tsx (BottomSheet 半屏/全屏), ChatMessageList.tsx (FlatList), ChatBubble.tsx (用户/AI 气泡), ChatInput.tsx (输入框+发送)
- F8.5 AppProvider 集成: ChatFAB + ChatPanel 放在 NavigationContainer 外层，chatService.initialize() 在 AppProvider useEffect 中调用
- F8.6 所有测试:
  - vibeflow-ios/__tests__/chat-store.test.ts: appendStreamDelta 拼接; finalizeStreamMessage 清空 streaming; sendMessage 追加用户消息; confirmToolCall 移除; openPanel/closePanel 切换
  - vibeflow-ios/__tests__/chat-service.test.ts (mock websocketService): sendMessage → sendEvent 被调用; CHAT_RESPONSE delta → appendStreamDelta; CHAT_RESPONSE complete → finalizeStreamMessage; CHAT_SYNC → messages 更新

注意:
- 使用 Expo SDK 54 + React Native + Zustand 模式
- 参考现有 vibeflow-ios/src/store/ 和 vibeflow-ios/src/services/ 的写法
- 完成后更新 docs/ai-chat-design/tasks.md 中 F8 的 checkbox 为 [x] 并附上 commit hash

确保 npx tsc --noEmit 通过，cd vibeflow-ios && npx jest 中新测试全绿后 commit。
