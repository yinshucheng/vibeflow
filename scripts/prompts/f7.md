切到 feature/f2-llm-engine 分支。
阅读 docs/ai-chat-design/tasks.md 中的「F7. 可观测性」部分 (F7.1-F7.3)。
阅读 docs/ai-chat-design/design.md 中与 Token 追踪 / LLMUsageLog / 可观测性相关的部分。

实现:
- F7.1 trackUsage(userId, conversationId, messageId, scene, modelId, usage): 写入 LLMUsageLog，计算 contextUsagePercent = contextLength / maxContextLimit * 100。每次 onFinish 回调中调用。
- F7.2 getConversationStats(userId, conversationId): 累计 tokens / 最新上下文使用率 / 消息数 / 当前模型。含 userId 所有权验证。
- F7.3 所有测试:
  - tests/services/chat-observability.test.ts: trackUsage 写入后记录存在且字段正确; contextUsagePercent 计算正确; getConversationStats 多次调用后 totalTokens = sum; 查询别人的 conversationId 返回错误
  - tests/property/chat-token-tracking.property.ts: 任意 inputTokens/outputTokens → totalTokens = input + output; contextUsagePercent 始终 >= 0

注意:
- Service 遵循 ServiceResult<T> 模式
- 使用 tests/helpers/chat-test-setup.ts (F0 已提供)
- 注册到 src/services/index.ts
- 完成后更新 docs/ai-chat-design/tasks.md 中 F7 的 checkbox 为 [x] 并附上 commit hash

确保 npx tsc --noEmit 通过，npm test 中新测试全绿后 commit。
